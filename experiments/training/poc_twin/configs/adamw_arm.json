{
  "arm": "adamw",
  "note": "Twin POC AdamW control arm (sweep OPT-2: old A2 default). Identical model, data order, schedule, grad clip, wd and QK-Clip as the Muon arm; ONLY the optimizer on hidden 2D weights differs.",
  "optimizer": {
    "all_params": {"type": "AdamW", "betas": [0.9, 0.95], "eps": 1e-8,
                   "weight_decay": 0.1, "lr": "probe winner from {1e-3, 2e-3, 4e-3}"}
  },
  "probe_grid": [0.001, 0.002, 0.004],
  "muon_arm_probe_grid": [0.01, 0.02, 0.04],
  "probe": "40 steps x 262k tokens, 5-step warmup, constant lr, ranked by eval loss on 32 fixed eval blocks",
  "see_also": "muon_arm.json for model/data/schedule/QK-Clip details"
}
