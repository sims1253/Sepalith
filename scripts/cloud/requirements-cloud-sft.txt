# Cloud mirror of .venv-sft (python 3.10.12) — pins lifted 2026-09-04 from the
# live venv (pip freeze) for Anyscale jobs. torch comes from plain PyPI (the
# CUDA runtime ships as nvidia-* wheel deps); no WSL2-only knobs needed on
# cloud Linux.
torch==2.11.0
unsloth==2026.8.18
trl==0.24.0
transformers==5.5.0
datasets==4.3.0
peft==0.20.0
accelerate==1.14.0
xformers==0.0.35
bitsandbytes==0.50.1
triton==3.6.0
torchao==0.18.0
numpy==2.2.6
tokenizers==0.22.2
huggingface_hub==1.27.0
gguf==0.19.0
